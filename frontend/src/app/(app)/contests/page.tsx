import { ContestListView } from "@/components/contests/ContestListView";

export const metadata = {
  title: "Contests — StonkSchool",
};

export default function ContestsPage() {
  return <ContestListView />;
}
